<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="assets/config/stylesheets/bobks.scss">
</head>

<body>
  <h1>Books</h1>
  
     
     <% @books.each do |book| %>
     <h4>Title</h4>
      <span> <%= link_to book.title, book_path(book.id) %></span>
     <% end %>
     
     <% @books.each do |book| %>
      <h4>Body</h4>
      <span> <%= book.body %></span>
     <% end %>
     
     
 </body>
 


